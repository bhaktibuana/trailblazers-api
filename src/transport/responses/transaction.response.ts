export class TransactionResponse {
	/**
	 * Calculate Gas Price - Transaction Response
	 *
	 * @param payload
	 * @returns
	 */
	public calculateGasPrice(payload: any | null) {
		if (!payload) return null;
		return payload;
	}
}
